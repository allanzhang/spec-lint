# Changelog

本文件记录 spec-lint 的版本变更。
版本号遵循语义化版本；规则库版本与技能版本同步。

## [1.7.0] - 2026-09-14

本版主题是**规则治理：先合并、再新增，清理重复规则**。

### 新增

- 文档模式识别：独立完整版 / 增量版 / 合并版，继承必须显式引用基线章节。
- C8「数据所有权 / 权威来源不明确」🟡 试运行，覆盖金额、税点、开票、授信、余额、权限和同步字段。
- 1-in-1-out、合并优先、规则健康表、合并/退役标准和季度体检。

### 变更

- A1 合并 B2：统一检查成功指标、验收标准、基线、目标值和数据来源。
- F1 合并 F2：普通 NFR 与高风险安全/合规合并，并支持基线继承。
- G3 合并 F3：状态生命周期与跨系统非原子失败统一评审。
- A6/A8 增加显式弃权：全部必做或不补备选方案时记录决策风险。
- B3 增加频次承诺五要素：时间、渠道、时区、停止条件和去重窗口。
- C1 增加动作词过载检查。
- `rules.json` 新增 `aliases` 和 `deprecated_rules`；`spec_lint.py stats` 支持别名映射。

### 移除

- 不再单独维护 B2、F2、F3 三条重复规则；保留历史 ID 别名。
- 活跃规则由 33 条降为 31 条，新增 C8 后规则总数没有增长。

## [1.6.0] - 2026-09-10

**首个正式发布版本**（此前仓库没有 tag 与 release，只能 `git clone`）。

v1.6.0 的主题是：**把评审从"一段中文报告"变成"一张下游能接得上的表"**。
原来的输出是散文，机器接不上，所以"查出来的问题变回下一轮要问的"这一步只能人工搬，
评审工具和上游提问环节形不成闭环。这一版把这条断点接上了。

### 新增

- **findings 表与输出契约**：评审产物是一张表，每条 finding 带规则 ID、严重度、行号、
  原文逐字引用、**下一轮该问什么**（`question`）和指纹（`fingerprint`）。
  契约写在 `SKILL.md` 里，任何 agent 读得到，不依赖脚本。
- **指纹 `fingerprint`**：`<rule_id>:<sha1(规范化证据)[:10]>`，只依赖规则 ID 与证据文本、
  **不依赖行号**。文档编辑导致行号漂移，两轮之间仍能对上号——这是循环能闭合的关键。
- **规则表机器可读化**：`references/rules.md` 每条规则补一行 `- 体裁：`；
  新增 `scripts/build_rules.py` 生成 `references/rules.json`（含严重度、适用体裁、
  判定方式、每条规则的「该问什么」），并带 `--check` 同步校验，防两处漂移。
- **四个模式**（`SKILL.md`）：
  1. 完整报告（默认分级清单）
  2. 门禁判定（通过 / 不通过 + 卡点清单）
  3. 复评循环（按指纹比对：已解决 / 仍存在 / 新出现）
  4. **开工前必答清单**——文档还不存在时，把规则库里的「请回答」印成开工前必答项，
     交给上游提问环节。spec-lint 只保证"该问的没漏"，不负责对话节奏。
- **`scripts/spec_lint.py`** 内部工具（可选加速器）：`scan` / `questions` / `round` / `stats`。
  有 shell + python3 时可把机械层做确定并自动补指纹；**没有也不影响评审**。
- **裁决账 `references/calibration.jsonl`**（一行一条，机器可读），与叙述性的
  `calibration.md` 并存。`stats` 据此自动计算规则体检，并按早已写定、但此前无人执行的
  生命周期阈值给出候选：
  | 信号 | 阈值 |
  |---|---|
  | 可升 blocker | 命中 ≥3 份不同文档 |
  | 降级候选 | **未修的**连续误报 ≥2 次 |
  | 重构候选 | 同一规则累计修复 ≥3 次（排除表在膨胀） |
  | 待删除评审 | ≥10 份文档零命中 |
- **项目级配置 `.spec-lint.json`**（模板见 `references/config-example.json`）：
  规则开关与严重度覆盖，以及**带理由、责任人、有效期的单条豁免**。
- **回归测试扩充**：`selftest.py` 在语料用例之外，新增工具链检查——规则表同步与完整性、
  指纹鲁棒性、证据强制、轮次对比。

### 变更

- **定位明确为 Skill**：入口是自然语言（"用 spec-lint 评审这份 PRD"），
  脚本是内部工具而非用法。此前 README 首屏与 SKILL.md 流程以命令行为主，属定位偏差。
- **「无证据不报警」由工具强制**：`scan --semantic` 会逐条核对 evidence 是否真在文档里，
  编造的、找不到原句的直接拒收；规则与体裁不匹配也拒收。行号给错会按证据自动定位。
- **误报处理方式改变**：不再"报个 ID 就全局退役"——一个人的误报不该让所有用户丢一条规则。
  改为项目级覆盖 + 带有效期的豁免，且豁免按指纹匹配，行号变化不会让它失效。
- **收敛信号改变**：从「无 🔴」改为「**本轮「新出现」为 0**」。
  理由是"还有没有问题"问题永远挑得出来，循环停不下来；而"有没有新问题"是可判定的。
- **报告定位改变**：报告是 findings 表的人读视图，不再是独立的数据源；
  Blocker 回应追踪表由 `round` 自动生成，不再手抄。
- **安装说明改为宿主中立**：原来写死 `~/.codex/skills/`，现按 Codex / Claude Code /
  DeepSeek Harness 分别列出。

### 移除

- `scan --fail-on` 与 `round --fail-on-new` 两个退出码参数。
  门禁判定已在表的 `gate` 字段、收敛在 `round` 的 `converged` 字段，agent 读表即可，
  退出码是多余的 CLI 面。

### 升级注意

- `references/rules.json` 是**生成物**，勿手改；改了 `rules.md` 要跑
  `python3 scripts/build_rules.py`，`selftest.py` 会校验两者同步。
- 首次发布，无历史 API 兼容负担。若你此前用过未发布的开发版并脚本化调用了
  `--fail-on`，改用读 findings 表的 `gate` 字段。

### 已知边界

- 33 条规则中，**机械层只覆盖 7 条**（A5 / C7 确定性命中，B1 / C3 / C6 / G1 给候选，
  C2 供交叉核对）；其余 26 条是语义判断，且多为「枚举完整性」类
  （状态机完备性、NFR 维度、经济假设推导、依赖失败模式…）——这类不主动逐条过一遍就必然漏。
- 门禁是**判断级**的：表与指纹让结论可追溯，但"够不够格开工"仍需人和流程确认。
- D2 / D4（需访问纪要、知识库原文）与 E 组（影响面）为 v2 预留，当前不应用、不报告。

## 未发布的开发历史

以下版本存在于 git 历史中，但没有 tag 与 release，仅作沿革记录。

### [1.5.0] - 2026-08-27

- 新增循环模式：生成 → 自检 → 修订 → 复审，上限 3 轮，收敛 = 无 🔴（或全部显式驳回）。
- 新增工作流串联定位图；README 改为串联版关系说明。
- 版本号统一；README 新增「接入 OpenSpec 流水线」——把门禁写进 `openspec/config.yaml`
  的 `rules:`，经 `openspec instructions` 注入模型上下文。

### [1.4.1] - 2026-08-27

- README 精简为定位页；规则表归一 `references/rules.md` 为唯一事实源。
- 修复 C3「我等一下」等待义漏判。
- 校准规则补场景声明；新增负例回归用例防排除表倒退；补维护机制条款。

### [1.4.0] - 2026-08-27

- 新增 H5「前置闸门与执行时序倒挂 / 验证计划名实不符」（试运行）。
- 调研/可行性文档复审反馈校准：B1「显著标识」法定术语、C3 引号内直接引语的「等」
  不再误报；D1 证据分级贯穿正文。

### [1.3.2] - 2026-08-27

- 仓库首个提交：PRD/Spec 质量评审技能，32 条规则。
- 新增 H 组（调研与可行性文档的「决策就绪度」标准）：调研文档不套 PRD 验收类规则。

> v1.3.1 及更早的规则演进（A/B/C/D/F/G 组成形、各条排除表的建立）不在本仓库 git 历史中，
> 详见 `references/calibration.md` 的校准记录。

[1.7.0]: https://github.com/allanzhang/spec-lint/releases/tag/v1.7.0
[1.6.0]: https://github.com/allanzhang/spec-lint/releases/tag/v1.6.0
