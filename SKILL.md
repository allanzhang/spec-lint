---
name: spec-lint
description: 评审产品需求文档（PRD / Spec）的质量门禁——检查缺失的成功指标、不可度量的目标、模糊表述、内部矛盾、占位残留等问题，输出带原文引用的质疑清单。当用户要求评审、lint、挑毛病、把关，或对起草中的文档做生成后自检/循环优化到可交付标准时使用。只评审，不代写、不改写文档。
metadata:
  short-description: PRD/Spec 质量评审门禁（只评审不代写）
---

# Spec Reviewer

对需求文档做"linter 式"评审：不评价商业判断，只保证决策依据是显形的、可度量的、自洽的。

**输出是一张表，不是一段报告。** 每条 finding 带规则 ID、稳定指纹、原文证据和「下一轮该问什么」——所以查出来的问题能直接变回上游要问的问题，两个工具才形成闭环。

## 铁律（不可违反）

1. **只评审，不代写**：输出质疑清单，绝不输出"帮你改好的文档"。循环模式（见 5.6）中修订动作由作者执行，本技能只负责判定、复评与收敛，始终不直接改文档。
2. **无证据不报警**：每条 finding 必须引用文档原句。**这条现在由工具强制**——`spec_lint.py scan --semantic` 会逐条核对 evidence 是否真在文档里，找不到就直接拒绝合并，编造的证据进不了表。推测性问题放入 `need_human`，不得混入硬伤。
3. **不确定就降级**：拿不准的问题严重度宁低勿高；误报比漏报更伤害这个工具的信誉。
4. **规则可关，但要留痕**：每条规则有 ID。关停/降级走项目级 `.spec-lint.json`；单条误报走带理由与有效期的 `waivers`。**不要"报个 ID 就全局退役"**——一个人的误报不该让所有用户丢一条规则。
5. **隐私铁律**：校准日志、tests 语料、commit message、README 中**永不出现**用户文档的公司名、项目名/产品名、人名、具体业务数据或原文片段；一律用体裁代称（"IoT 智能硬件 PRD""SaaS 工程 PRD"）。本仓库公开分发，任何写入仓库的内容都视同对外发布。

## 评审流程

0. **判断体裁，选择评审标准**（体裁决定跑哪些规则，工具按 genre 过滤）：
   - **PRD / 工程规格** → `--genre prd`，用"可验收性"标准：指标可度量、场景有边界、状态机完备（A/B/C/D/F/G 组）；
   - **调研 / 可行性评估** → `--genre research`，用"决策就绪度"标准：假设可追溯、经济模型自洽、风险有闭环、结论不超前于论证（H 组）。这类文档**不套**验收类规则；
   - 混合文档：分章节按各自标准评审，在报告中注明。
1. **获取文档，归一化为 Markdown 文本**：直接输入 `.md`/`.txt` 直接用；`.docx`/`.pdf`/`.html` 先用当前环境可用工具转 Markdown 再评，并告知用户"基于转换后文本评审，表格/排版可能有损耗"。飞书链接是**可选专项分支**（给链接时才走 lark-doc），不可用时退回"请导出 Markdown/Word"，不影响主流程。
2. **机械 + 候选检查（工具）**：
   ```bash
   python3 scripts/spec_lint.py scan <文档> --genre prd --out findings.json
   ```
   工具读 `references/rules.json` 决定每条规则的严重度、适用体裁和「该问什么」。其中：
   - **mechanical**（A5/C7）命中即结论；
   - **hybrid**（B1/C3/C6/G1）只给候选，必须结合上下文确认；
   - **helper**（C2）只输出含数字的行供交叉核对，本身不构成 finding。
3. **语义检查**：读 `references/rules.md`，逐条应用 A/B/C/D/F/G/H 组规则，把确认的结论写成 `semantic.json`，再合并：
   ```json
   {"findings": [{"rule_id": "A1", "line": 7, "evidence": "<原文逐字>", "section": "目标"}],
    "need_human": ["<位置 + 疑点 + 为什么不确定>"]}
   ```
   ```bash
   python3 scripts/spec_lint.py scan <文档> --genre prd --semantic semantic.json --out findings.json
   ```
   工具会核对证据、按体裁校验规则、自动补上严重度与「该问什么」，并按证据定位行号（给错行号也能纠正）。
4. **出报告**：按 `references/report-format.md` 渲染中文报告。**报告是 findings 表的人读视图**，不是另一份数据源。
5. **给结论**：一句话——当前是否具备进入研发评审的条件。表里的 `gate` 字段已给出判定（无未解决 blocker = pass）。
5.5. **门禁模式**：`scan ... --fail-on blocker` 退出码 1 = 不通过，可直接接 CI。输出收敛为「通过 / 不通过 + 🔴 卡点清单 + 一句话结论」。
5.6. **循环模式（起草/生成中场景的默认出口）**：用 `round` 当发动机。
   - 第 1 轮：完整 `scan` 得 `r1.json`；把表里每条的 `question` 交给作者回答、修订文档；
   - 第 N 轮：`scan <文档> --round N --out rN.json`
   - 复评：`python3 scripts/spec_lint.py round --prev r{N-1}.json --current rN.json`
   - **收敛信号 = 本轮「新出现」为 0**，不是「还有没有问题」——问题永远挑得出来，永远能挑；
   - 门禁通过 = 无未解决 blocker（已豁免的自动扣掉）；
   - 上限 3 轮：仍不收敛则冻结循环，回到第 6 步人工裁决，并如实标注"未达门禁标准即交付"；
   - 修订永远是作者动作。**评审者与作者不要是同一个上下文**，否则是自己给自己打分。
6. **征求裁决（进化环节，必做）**：报告末尾固定询问："以上哪条你不认可（误报）？有没有漏掉的问题（漏报）？哪条级别不对？" 用户裁决后：
   - 把裁决写进 `references/calibration.jsonl`（一行一条 verdict，机器可读），叙述性教训同时写进 `references/calibration.md`；
   - `python3 scripts/spec_lint.py stats` 会自动算命中/误报/已修/漏报/未修连续，并按阈值给出升降级候选；
   - 据此**起草**规则/脚本改动，先在工作副本修改并运行 `python3 scripts/selftest.py` 回归；
   - 把改动摘要和自测结果给用户过目，**等用户明确确认后**才同步到宿主技能目录并提交推送——不得自行安装或推送；
   - 新规则先以 🟡/🔵 或「需人工确认」身份试运行（生命周期见 calibration.md）。

## 文档还没写的时候：印提问清单

需求澄清阶段文档尚不存在时，本技能不评文档，改为**印提问清单**：

```bash
python3 scripts/spec_lint.py questions --genre prd               # 人读的必答清单
python3 scripts/spec_lint.py questions --genre research --json    # 交给上游提问工具
```

它把每条规则的「请回答」印成开工前的必答清单，按体裁过滤。**提问由上游（brainstorming 一类）执行**，本技能只提供问题的完备性——保证该问的没漏。本技能不负责对话、追问与节奏。

## 工作流位置（串联定位）

```
① brainstorming（发散：意图/假设/边界/取舍）        ← 上游，负责"问"
② 成文 Spec
③ spec-lint 出表：编号 + 原话 + 下一轮该问什么        ← 本技能，负责"查"
④ 表里的问题变回上游要问的 → 修订 → 复审
   收敛信号：本轮「新出现」为 0                      ← round
⑤ 过门 → writing-plans → 审批 → 执行
```

配合循环模式，本技能从"事后评审"变为"生成内嵌自检"：烂 Spec 一律拦在写计划和研发之前。

## 回归测试

改动机械检查器、规则或工具后，必须运行 `python3 scripts/selftest.py`。它跑两部分：

1. **语料回归**：`tests/cases/*.md` 对照 `tests/expectations/*.json`（应命中的埋雷样例、不应误报的片段、决策标记样例）。新增误报模式时把反例补进 02 号用例；新增规则时补正例用例。真实评审过的文档脱敏后可存入 `tests/cases/` 扩充语料。
2. **工具链回归**：规则表与 rules.md 是否同步、规则表完整性、指纹鲁棒性、证据强制是否生效、轮次对比是否正确。

## 范围边界

- v1.6 应用 A（完整性）、B（可度量性）、C（歧义自洽）、D（证据）、F（非功能与上线就绪）、G（决策归集与可行性）、H（调研决策就绪度）共 33 条规则，全部基于文档本身，零外部依赖，只依赖 `python3` 标准库。
- 规则唯一事实源是 `references/rules.md`；`references/rules.json` 是 `scripts/build_rules.py` 的生成物，勿手改。
- D1 与 H 组已生效；D2/D4（需访问纪要、知识库原文）与 E 组（影响面）在 `references/rules.md` 中列为 v2 预留，**当前不应用、不报告**。
- 不评分、不排名、不自动改写、不评价"该不该做这个功能"。
- 门禁是**提示级 + 工具级**：表、指纹、退出码是硬的；「该不该动手改」仍是人的判断。
