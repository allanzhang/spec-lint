---
name: spec-lint
description: 评审产品需求文档（PRD / Spec）的质量门禁——检查缺失的成功指标、不可度量的目标、模糊表述、内部矛盾、占位残留等问题，输出带原文引用的质疑清单。当用户要求评审、lint、挑毛病、把关一份 PRD/需求文档/产品方案时使用。只评审，不代写、不改写文档。
metadata:
  short-description: PRD/Spec 质量评审门禁（只评审不代写）
---

# Spec Reviewer

对需求文档做"linter 式"评审：不评价商业判断，只保证决策依据是显形的、可度量的、自洽的。

## 铁律（不可违反）

1. **只评审，不代写**：输出质疑清单，绝不输出"帮你改好的文档"。
2. **无证据不报警**：每条 finding 必须引用文档原句并标注位置（章节/行号）。推测性问题放入「需人工确认」，不得混入硬伤。
3. **不确定就降级**：拿不准的问题严重度宁低勿高；误报比漏报更伤害这个工具的信誉。
4. **规则可关**：每条规则有 ID，用户可要求禁用或降级某条；报告末尾列出本次启用的规则。
5. **隐私铁律**：校准日志、tests 语料、commit message、README 中**永不出现**用户文档的公司名、项目名/产品名、人名、具体业务数据或原文片段；一律用体裁代称（"IoT 智能硬件 PRD""SaaS 工程 PRD"）。本仓库公开分发，任何写入仓库的内容都视同对外发布。

## 评审流程

0. **判断体裁，选择评审标准**：
   - **PRD / 工程规格**（需求设计、功能 Spec）：用"可验收性"标准——指标可度量、场景有边界、状态机完备（A/B/C/D/F/G 组规则）；
   - **调研 / 可行性评估**（市场分析、立项报告）：用"决策就绪度"标准——假设可追溯、经济模型自洽、风险有闭环、结论不超前于论证（H 组规则）。这类文档**不套**验收类规则（如要求每个功能有验收用例）；
   - 混合文档（调研 + 产品建议）：分章节按各自标准评审，在报告中注明。
1. **获取文档，统一归一化为 Markdown 文本**（本技能不依赖任何外部集成即可完成评审）：
   - 直接输入：用户粘贴的文本、本地 `.md`/`.txt` 文件——直接使用。
   - 其他可解析格式（`.docx`、`.pdf`、`.html` 等）：先用当前环境可用的工具转换为 Markdown（如文档/PDF 类技能或转换工具），存入临时文件后再评审；转换后告知用户"基于转换后的文本评审，表格/排版可能有损耗"。
   - 飞书链接是**可选专项分支**：仅当用户给出飞书文档链接时，才调用 lark-doc 技能读取为文本；lark 不可用或用户拒绝时，退回"请用户导出为 Markdown/Word"，不影响主流程。
2. **机械检查**：对归一化后的 Markdown 文件运行 `scripts/check_mechanical.py <文件路径>`，得到带行号的确定性结果（A5 占位符、B1 模糊词、C3 开放列举、C6 弱模态、C7 和/或、G1 决策点标记、含数字行）。脚本输出 JSON。引用行号时以归一化后的文本为准。
3. **语义检查**：阅读 `references/rules.md`，逐条应用 A（完整性）、B（可度量性）、C（歧义自洽）、D（证据）、F（非功能与上线就绪）、G（决策归集与可行性）、H（调研决策就绪度）八组规则。脚本的结果作为证据输入，不替代语义判断（例如模糊词需结合上下文确认是否真的无度量主体）。
4. **合并出报告**：按 `references/report-format.md` 的模板输出中文报告。
5. **给结论**：摘要区给出一句话判断——该文档当前是否具备进入研发评审的条件。
5.5. **门禁模式（可选）**：当用户要求"门禁判定/能否进入评审"时，输出收敛为「通过 / 不通过 + 🔴 卡点清单 + 一句话结论」，不输出完整分级报告；未指定时默认输出完整报告。
6. **征求裁决（进化环节，必做）**：报告末尾固定询问："以上哪条你不认可（误报）？有没有漏掉的问题（漏报）？哪条级别不对？" 用户裁决后：
   - 将裁决按 `references/calibration.md` 的格式追加到校准日志（FP/MISS/SEV + 现象）；
   - 据此**起草**规则/脚本改动，先在工作副本修改并运行 `python3 scripts/selftest.py` 回归；
   - 把改动摘要和自测结果给用户过目，**等用户明确确认后**才同步到 `~/.codex/skills/` 并提交推送——不得自行安装或推送。
   - 新规则先以 🟡/🔵 或「需人工确认」身份试运行（生命周期见 calibration.md）。

## 回归测试

改动机械检查器或规则后，必须运行 `python3 scripts/selftest.py`：tests/cases/ 是语料库（含应命中的埋雷样例、不应误报的工程规格片段、决策标记样例），tests/expectations/ 是断言。新增误报模式时，把该反例补进 02 号用例；新增规则时，补正例用例。真实评审过的文档脱敏后可存入 tests/cases/ 扩充语料。

## 范围边界

- v1 应用 A（完整性）、B（可度量性）、C（歧义自洽）、D（证据）、F（非功能与上线就绪）、G（决策归集与可行性）、H（调研决策就绪度）共 33 条规则，全部基于文档本身，零外部依赖。每条规则在 rules.md 中标注了出处（IEEE 830 / spec-kit clarify / OpenSpec / Amazon PR-FAQ / 歧义研究 / 经验先验 / 真实项目校准）。
- D1（文档内可判定的证据断言）与 H 组（调研决策就绪度）已 v1 生效；D2/D4（需访问纪要、知识库原文）与 E 组（影响面）规则在 `references/rules.md` 中列为 v2 预留，**当前不应用、不报告**，避免无数据源时产生空想式质疑。
- 不评分、不排名、不自动改写、不评价"该不该做这个功能"。
