---
name: spec-lint
description: 评审产品需求文档（PRD / Spec）的质量门禁——检查缺失的成功指标、不可度量的目标、模糊表述、内部矛盾、占位残留等问题，输出带原文引用的质疑清单。当用户要求评审、lint、挑毛病、把关，对起草中的文档做自检循环，或在需求尚未成文时列出开工前必须回答的问题时使用。只评审，不代写、不改写文档。
metadata:
  short-description: PRD/Spec 质量评审门禁（只评审不代写）
---

# Spec Reviewer

对需求文档做"linter 式"评审：不评价商业判断，只保证决策依据是显形的、可度量的、自洽的。

**这是一个技能，入口是自然语言。** 用户在 agent 里说"用 spec-lint 评审这份 PRD"——你读完本文档就能开始，**不依赖任何命令行**。`scripts/` 下的脚本是**可选加速器**：有 shell 和 python3 时可以用它们把机械层做确定；没有也完全不影响评审本身。

## 两种用法

**A. 单独用——评估一份已有需求。** 用户给你一份 PRD / 调研文档（粘贴文本、本地文件、或飞书链接）。走完「评审流程」，产出报告 + findings 表。

**B. 接入 pipeline——从零开发一个项目。**

```
① 只有想法、还没有文档   → 出「开工前必答清单」，交给上游提问环节
② 成文 spec / proposal   → 门禁判定（通过 / 不通过 + 卡点）
③ 修订 → 复评（按指纹比对上一轮）→ 收敛
④ 过门                   → 交给写计划 / 执行环节
```

Pipeline 的黏合剂是 findings 表的 `fingerprint`：它让"上一轮问过的"和"这一轮查出的"能对上号。

## 铁律（不可违反）

1. **只评审，不代写**：输出质疑清单，绝不输出"帮你改好的文档"。循环复评中修订动作由作者执行，本技能只负责判定、复评与收敛。
2. **无证据不报警**：每条 finding 必须引用文档原句。**找不到原句的质疑不允许上报**，推测性问题只能进「需人工确认」。用脚本时这条会被逐条强制核对，编造的证据直接拒收。
3. **不确定就降级**：拿不准的问题严重度宁低勿高；误报比漏报更伤害这个工具的信誉。
4. **规则可关，但要留痕**：每条规则有 ID。关停/降级走项目级 `.spec-lint.json`；单条误报走带理由与有效期的 `waivers`。**不要"报个 ID 就全局退役"**——一个人的误报不该让所有用户丢一条规则。
5. **隐私铁律**：校准日志、tests 语料、commit message、README 中**永不出现**用户文档的公司名、项目名/产品名、人名、具体业务数据或原文片段；一律用体裁代称（"IoT 智能硬件 PRD""SaaS 工程 PRD"）。本仓库公开分发，任何写入仓库的内容都视同对外发布。
6. **治理优先**：新增规则前必须先做重叠检查；原则上执行 1-in-1-out，合并、降级或退役至少一条旧规则后才能新增。禁止把同一失败机理按行业或项目重复登记。

## 输出契约：findings 表

评审的产物是**一张表**，不是一段散文——因为 pipeline 的下一步要读它。

| 字段 | 要求 |
|---|---|
| `rule_id` | 规则 ID，必须来自 `references/rules.md` |
| `severity` | `blocker` / `warning` / `nit`。默认取规则表，项目配置可覆盖 |
| `line` | 原文所在行号（以归一化后的文本为准） |
| `evidence` | 文档原句**逐字**引用，不得改写 |
| `question` | 该规则对应的「请作者回答什么」。**下一轮上游要问的就是这句** |
| `fingerprint` | `<rule_id>:<hash>`，算法见下 |
| `status` | `open` 未解决 / `answered` 已回答 / `waived` 显式豁免（问题成立但接受） / `dismissed` 驳回（机械误报，问题不成立） |

**指纹算法**（跨轮次、跨实现都要一致）：

```
规范化(text) = 去掉全部空白字符，再去掉首尾的常见中英文标点
fingerprint  = "<rule_id>:" + sha1(规范化(evidence) + "|" + 规范化(match 或 ""))[:10]
```

它**只依赖规则 ID 与证据文本，不依赖行号**——文档编辑导致行号漂移，两轮之间仍然对得上号。
这是循环能闭合的关键，也是它比一段报告值钱的地方。

## 评审流程

0. **判断体裁和文档模式**（决定用哪套标准、跑哪些规则）：
   - **PRD / 工程规格** → 「可验收性」：指标可度量、场景有边界、状态机完备（A/B/C/D/F/G 组）
   - **调研 / 可行性 / 立项** → 「决策就绪度」：假设可追溯、经济模型自洽、风险有闭环、结论不超前于论证（H 组）。这类文档**不套**验收类规则
   - 混合文档：分章节按各自标准评，报告里注明
   - **独立完整版**：背景、目标、非目标、指标、非功能、安全、验收和状态机必须在本文自证
   - **增量版**：可显式继承基线，但必须能定位继承章节；新增风险不能被“继承”吞掉
   - **合并版**：必须标明当前有效部分和历史追溯部分，规则只以有效部分为准
   - 规则表里每条都标了适用体裁，按体裁取子集
1. **归一化为 Markdown 文本**：`.md`/`.txt` 直接用；`.docx`/`.pdf`/`.html` 先用当前环境可用工具转换，并告知用户"基于转换后文本评审，表格/排版可能有损耗"。飞书链接是**可选分支**（给了链接才走 lark-doc），不可用时退回"请导出 Markdown/Word"，不影响主流程。
2. **机械 / 候选检查**：读 `references/rules.json`（或 `rules.md`）拿规则表，逐条应用：
   - **A5 占位残留、C7 和/或** —— 确定性命中即结论
   - **B1 模糊词、C3 开放列举、C6 弱模态、G1 决策点** —— 只是候选，**必须结合上下文确认**。别把「幂等」的等、「隐私友好」、「智能化平台」这类固定术语当问题报
   - **C2 数字矛盾** —— 先把含数字的行列出来，再自查交叉一致
   - **其余 26 条是纯语义判断**，且多为「枚举完整性」类（状态机完备性、NFR 维度、经济假设推导、依赖失败模式…）——**这类提示不主动过一遍就必然漏**，逐条走
   - *可选加速*：`python3 scripts/spec_lint.py scan <文档> --genre prd --out findings.json`
3. **写出 findings 表**：按上面的契约，每条都要有 `evidence` 和 `question`。机械层只给候选，**判断权在你**，所以还有一个 `resolutions` 层用来裁定候选：

   ```json
   {
     "findings": [{"rule_id": "A1", "line": 7, "evidence": "<原文逐字>", "section": "目标"}],
     "resolutions": [
       {"rule_id": "B1", "severity": "warning", "note": "为什么降级"},
       {"rule_id": "C3", "evidence": "<原文子串>", "status": "dismissed", "reason": "为什么是误报"}
     ],
     "need_human": ["<位置 + 疑点 + 为什么不确定>"]
   }
   ```

   选择器：`rule_id` 必填；`evidence`（子串）/ `line` / `match` 任选其一用于定位，都不给则作用于该规则的全部候选。**没有这一层，机械层的误报会永久污染门禁判定**。驳回（`dismissed`）必须写 `reason`——它会成为校准日志里的 FP 记录，规则靠它收敛。
4. **渲染报告**：按 `references/report-format.md`。**报告是表的人读视图**，不是另一份数据源。
5. **给结论**：一句话——当前是否具备进入研发评审的条件。
6. **征求裁决（必做）**：报告末尾固定询问："以上哪条你不认可（误报）？有没有漏掉的问题（漏报）？哪条级别不对？" 用户裁决后：
   - 裁决写进 `references/calibration.jsonl`（一行一条，机器可读），叙述性教训写进 `references/calibration.md`
   - 规则改动先在工作副本改，跑 `python3 scripts/selftest.py` 回归，**等用户明确确认后**才提交推送

## 四个模式

### 1. 完整报告（默认）
分级列出 blocker / warning / nit，加「需人工确认」区。

### 2. 门禁判定（用户问"能不能进入研发评审"时）
输出收敛为「**通过 / 不通过 + 卡点清单 + 一句话结论**」。不通过 = 还有未解决的 blocker（已显式豁免的算已解决）。

### 3. 复评 / 循环（文档在起草或 AI 协作生成中）
- 每轮**只复评**两件事：上一轮的问题解决了没 + 本轮改动有没有带出**新**问题
- 按 `fingerprint` 比对，分「已解决 / 仍存在 / 新出现」
- **收敛信号 = 本轮「新出现」为 0**，不是「还有没有问题」——问题永远挑得出来，永远能挑
- 上限 3 轮；仍不收敛则冻结循环，交人工裁决，并如实标注"未达门禁标准即交付"
- 修订永远是作者的动作。**评审者与作者不要是同一个上下文**，否则是自己给自己打分
- *可选加速*：`python3 scripts/spec_lint.py round --prev <上轮表> --current <本轮表>`

### 4. 开工前必答清单（文档还不存在时）
需求还在想法阶段，或用户问"我该写清楚什么"时——**不要评文档，改为印清单**：把该体裁下每条规则的「请回答」列出来，组成开工前的必答项。

- 内容直接来自 `references/rules.json` 的 `question` 字段（每条规则都有），**不凭记忆列举**
- **清单由你印，提问由上游执行**：你只保证"该问的没漏"，不负责对话节奏与追问
- *可选加速*：`python3 scripts/spec_lint.py questions --genre prd`

## 工作流位置（串联定位）

```
① brainstorming（发散：意图/假设/边界/取舍）      ← 上游，负责"问"
② 成文 Spec / proposal
③ spec-lint 出表：编号 + 原话 + 下一轮该问什么     ← 本技能，负责"查"
④ 表里的问题变回上游要问的 → 修订 → 复评
   收敛信号：本轮「新出现」为 0
⑤ 过门 → writing-plans → 审批 → 执行
```

配合循环模式，本技能从"事后评审"变为"生成内嵌自检"：烂 Spec 一律拦在写计划和研发之前。

## 维护者工具（不是给用户用的入口）

以下只在改规则库 / 修工具时才需要：

| 命令 | 用途 |
|---|---|
| `python3 scripts/build_rules.py` | 改了 `rules.md` 后重新生成 `rules.json`（生成物，勿手改） |
| `python3 scripts/selftest.py` | 回归：语料用例 + 工具链（规则表同步/完整性、指纹鲁棒性、证据强制、轮次对比） |
| `python3 scripts/spec_lint.py stats` | 规则体检：命中/误报/已修/漏报，按阈值给出升降级与重构候选 |

规则唯一事实源是 `references/rules.md`。新增规则先以 🟡/🔵 或「需人工确认」身份试运行；阈值与生命周期见 `references/calibration.md`。

## 范围边界

- 应用 A（完整性）、B（可度量性）、C（歧义自洽）、D（证据）、F（非功能与上线就绪）、G（决策归集与可行性）、H（调研决策就绪度）共 31 条活跃规则，另有 3 条合并别名（B2→A1、F2→F1、F3→G3），全部基于文档本身，零外部依赖。
- D1 与 H 组已生效；D2/D4（需访问纪要、知识库原文）与 E 组（影响面）列为 v2 预留，**当前不应用、不报告**。
- 不评分、不排名、不自动改写、不评价"该不该做这个功能"。
- 门禁是**判断级**的：表与指纹让结论可追溯，但「够不够格开工」仍需人和流程确认。
